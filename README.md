# Deployment-of-3-tier-Applications-Using-AWS-Services
This project demonstrates a manually deployed 3-tier architecture on AWS for a web application where users can submit data, which is then stored in a backend database. This setup relies on AWS native services for Hosting, Networking and Storage.
